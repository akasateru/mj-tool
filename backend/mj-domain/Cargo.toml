[package]
name = "mj-domain"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
riichi-tools-rs = "0.0.70"

[dev-dependencies]
rstest = "0.21"

