# ルートの .env を Backend / Frontend 共通で使用する
# 起動前に読み込む: set -a && source .env && set +a

# Backend (Rust / mj-api)
PORT=8080
DATABASE_URL=sqlite://./mj.sqlite?mode=rwc

# Frontend (Next.js)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
